export const add = (x, y) => {
  return x + y;
};

export const substract = (x, y) => {
  return x - y;
};

export const divide = (x, y) => {
  return x / y;
};

export const multiply = (x, y) => {
  return x * y;
};
